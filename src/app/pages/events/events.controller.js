(function() {
    'use strict';
    
    angular
        .module('after')
        .controller('EventsController', EventsController);
    
        /** @ngInject */
        function EventsController($scope) {
            var ctrl = this;
            
            activate();
            
            function activate() {
            
            }
        }
})();

(function() {
    'use strict';
    
    angular
        .module('after')
        .controller('EventController', EventController);

        /** @ngInject */
        function EventController($scope) {
            var ctrl = this;
            
            activate();
            
            function activate() {
            
            }
        }
})();
